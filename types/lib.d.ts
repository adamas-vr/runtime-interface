// Generated by dts-bundle-generator v9.5.1

import { UUID } from 'crypto';
import { quat, vec2, vec3, vec4 } from 'gl-matrix';

/**
 * Entity management APIs for creating, destroying, and updating entities.
 *
 * @module entity
 */
/**
 * Opaque numeric handle that identifies an entity.
 *
 * Values of this type are returned by entity creation APIs and consumed by other
 * entity management operations.
 */
export type Entity = number;
/**
 * Creates and manages entities.
 */
export declare class EntityManager {
	/**
	 * Creates a new entity.
	 *
	 * @param name - Human-readable name to assign to the new entity.
	 * @returns A promise that resolves to the newly created {@link Entity} handle.
	 */
	static Create(name: string): Promise<number>;
	/**
	 * Destroys an existing entity.
	 *
	 * @param entity - The {@link Entity} handle to remove from the runtime.
	 * @returns A promise that resolves to `true` when the entity was destroyed
	 * successfully, or `false` when the destroy operation was rejected or failed.
	 */
	static Destroy(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether an entity is active.
	 *
	 * @param entity - The {@link Entity} handle to update.
	 * @param active - `true` to activate the entity; `false` to deactivate it.
	 * @returns A promise that resolves when the active state has been changed.
	 */
	static SetActive(entity: Entity, active: boolean): Promise<void>;
	/**
	 * Retrieves whether an entity is currently active.
	 *
	 * @param entity - The {@link Entity} handle to inspect.
	 * @returns A promise that resolves to `true` if the entity is active, otherwise
	 * `false`.
	 */
	static GetActive(entity: Entity): Promise<boolean>;
	/**
	 * Set the name of an entity.
	 *
	 * @param entity - The {@link Entity} to rename.
	 * @param name - The new human-readable name for the entity.
	 * @returns A promise that resolves when the name has been changed.
	 */
	static SetName(entity: Entity, name: string): Promise<void>;
	/**
	 * Gets the name of an entity.
	 *
	 * @param entity - The {@link Entity} whose name should be returned.
	 * @returns A promise that resolves to the entity's current name.
	 */
	static GetName(entity: Entity): Promise<string>;
}
/**
 * Opaque numeric handle that identifies a texture.
 */
export type Texture = number;
/**
 * Supported texture data formats.
 */
export declare enum TextureFormat {
	/** Alpha-only texture format, 8 bit integer. */
	Alpha8 = 1,
	/** Three channel RGB texture format, 8-bits unsigned integer per channel. */
	RGB24 = 3,
	/** Four channel RGBA texture format, 8-bits unsigned integer per channel. */
	RGBA32 = 4
}
/**
 * Supported render texture formats.
 */
export declare enum RenderTextureFormat {
	/** Depth render texture format. */
	Depth = 1,
	/** Shadow map render texture format. */
	Shadowmap = 3,
	/** Default color render texture format. */
	Default = 7,
	/** Default HDR color render texture format. */
	DefaultHDR = 9
}
/**
 * Supported texture dimensions.
 */
export declare enum TextureDimension {
	/** 2D texture. */
	Tex2D = 2,
	/** Cubemap texture.  */
	Cube = 4
}
/**
 * Texture filtering modes.
 */
export declare enum TextureFilterMode {
	Nearest = 0,
	Linear = 1,
	NearestMipmapNearest = 2,
	LinearMipmapNearest = 3,
	NearestMipmapLinear = 4,
	LinearMipmapLinear = 5
}
/**
 * Texture wrapping modes.
 */
export declare enum TextureWrapMode {
	ClampToEdge = 0,
	Repeat = 1,
	MirroredRepeat = 2
}
/**
 * Image data returned from a texture readback operation.
 */
export interface ImageReadbackResult {
	/** Width of the image, in pixels. */
	width: number;
	/** Height of the image, in pixels. */
	height: number;
	/** Encoding of the returned image data. */
	kind: "rgba" | "png" | "jpeg";
	/** Image data. */
	data: Uint8Array;
}
/**
 * Creates, updates, and reads textures.
 */
export declare class TextureManager {
	/**
	 * Creates a 2D texture.
	 *
	 * @param width - The texture width, in pixels.
	 * @param height - The texture height, in pixels.
	 * @param format - The texture format. Defaults to {@link TextureFormat.RGBA32}.
	 * @param linear - Whether the texture should use linear color format. Defaults to
	 * `false` (sRGB).
	 * @returns A promise that resolves to the created {@link Texture}.
	 */
	static Create2D(width: number, height: number, format?: TextureFormat, linear?: boolean): Promise<number>;
	/**
	 * Creates a render texture.
	 *
	 * @param width - The texture width, in pixels. Defaults to `512`.
	 * @param height - The texture height, in pixels. Defaults to `512`.
	 * @param depth - The depth buffer size. Defaults to `16`.
	 * @param dimension - The texture dimension. Defaults to
	 * {@link TextureDimension.Tex2D}.
	 * @param format - The texture format. Defaults to
	 * {@link RenderTextureFormat.Default}.
	 * @returns A promise that resolves to the created {@link Texture}.
	 */
	static CreateRenderTexture(width?: number, height?: number, depth?: number, dimension?: TextureDimension, format?: RenderTextureFormat): Promise<number>;
	/**
	 * Destroys a texture.
	 *
	 * @param handle - The {@link Texture} to destroy.
	 * @returns A promise that resolves to `true` if the texture was destroyed, or
	 * `false` otherwise.
	 */
	static Destroy(handle: Texture): Promise<boolean>;
	/**
	 * Gets the size of a texture.
	 *
	 * @param handle - The {@link Texture} to inspect.
	 * @returns A promise that resolves to the texture size as `[width, height]`.
	 */
	static GetTextureSize(handle: Texture): Promise<vec2>;
	/**
	 * Sets raw RGBA image data for a texture.
	 *
	 * @param handle - The {@link Texture} to update.
	 * @param rgbaData - RGBA byte data in `R`, `G`, `B`, `A` order.
	 * @param width - The image width, in pixels.
	 * @param height - The image height, in pixels.
	 * @returns A promise that resolves when the image data has been changed.
	 */
	static LoadRGBAImage(handle: Texture, rgbaData: Uint8Array, width: number, height: number): Promise<void>;
	/**
	 * Loads encoded image data into a texture.
	 *
	 * @param handle - The {@link Texture} to update.
	 * @param image - PNG, JPG, or EXR image data.
	 * @returns A promise that resolves when the image data has been changed.
	 */
	static LoadImage(handle: Texture, image: Uint8Array): Promise<void>;
	/**
	 * Gets image data from a texture in RGBA format.
	 *
	 * @param handle - The {@link Texture} to read.
	 * @returns A promise that resolves to the image data.
	 */
	static ReadbackRGBAImage(handle: Texture): Promise<ImageReadbackResult>;
	/**
	 * Gets image data from a texture in JPEG format.
	 *
	 * @param handle - The {@link Texture} to read.
	 * @param quality - The JPEG quality, from `0` to `100`. Defaults to `75`.
	 * @returns A promise that resolves to the image data.
	 */
	static ReadbackJPGImage(handle: Texture, quality?: number): Promise<ImageReadbackResult>;
	/**
	 * Gets image data from a texture in PNG format.
	 *
	 * @param handle - The {@link Texture} to read.
	 * @returns A promise that resolves to the image data.
	 */
	static ReadbackPNGImage(handle: Texture): Promise<ImageReadbackResult>;
	/**
	 * Sets the filter mode of a texture.
	 *
	 * @param handle - The {@link Texture} to update.
	 * @param mode - The filter mode to apply.
	 * @returns A promise that resolves when the filter mode has been changed.
	 */
	static SetFilterMode(handle: Texture, mode: TextureFilterMode): Promise<void>;
	/**
	 * Sets the wrap mode of a texture on the U axis.
	 *
	 * @param handle - The {@link Texture} to update.
	 * @param wrapMode - The wrap mode to apply.
	 * @returns A promise that resolves when the wrap mode has been changed.
	 */
	static SetWrapModeU(handle: Texture, wrapMode: TextureWrapMode): Promise<void>;
	/**
	 * Sets the wrap mode of a texture on the V axis.
	 *
	 * @param handle - The {@link Texture} to update.
	 * @param wrapMode - The wrap mode to apply.
	 * @returns A promise that resolves when the wrap mode has been changed.
	 */
	static SetWrapModeV(handle: Texture, wrapMode: TextureWrapMode): Promise<void>;
}
/**
 * Opaque numeric handle that identifies a material.
 */
export type Material = number;
/**
 * Supported material properties.
 */
export declare enum MaterialProperty {
	/** Base color as a `vec4`, with each channel in the range `[0.0, 1.0]`. */
	BaseColor = "baseColorFactor",
	/** Base color texture. */
	BaseColorMap = "baseColorTexture",
	/** Base color texture scale and offset as a `vec4`. */
	BaseColorMapScaleOffset = "baseColorTexture_ST",
	/** Base color texture rotation as a non-negative number in `[0, +inf]`. */
	BaseColorMapRotation = "baseColorTextureRotation",
	/** Normal map strength in the range `[0.0, 2.0]`. */
	NormalScale = "normalScale",
	/** Normal map texture. */
	NormalMap = "normalTexture",
	/** Normal map texture scale and offset as a `vec4`. */
	NormalMapScaleOffset = "normalTexture_ST",
	/** Normal map texture rotation as a non-negative number in `[0, +inf]`. */
	NormalMapRotation = "normalTextureRotation",
	/** Emission color as a vector with values in the range `[0.0, +inf]`. */
	Emission = "emissiveFactor",
	/** Emission texture. */
	EmissionMap = "emissiveTexture",
	/** Emission texture scale and offset as a `vec4`. */
	EmissionMapScaleOffset = "emissiveTexture_ST",
	/** Emission texture rotation as a non-negative number in `[0, +inf]`. */
	EmissionMapRotation = "emissiveTextureRotation",
	/** Occlusion strength in the range `[0.0, 1.0]`. */
	OcclusionStrength = "occlusionStrength",
	/** Occlusion texture. */
	OcclusionMap = "occlusionTexture",
	/** Occlusion texture scale and offset as a `vec4`. */
	OcclusionMapScaleOffset = "occlusionTexture_ST",
	/** Occlusion texture rotation as a non-negative number in `[0, +inf]`. */
	OcclusionMapRotation = "occlusionTextureRotation",
	/** Metallic value in the range `[0.0, 1.0]`. */
	Metalness = "metallicFactor",
	/** Roughness value in the range `[0.0, 1.0]`. */
	Roughness = "roughnessFactor",
	/** Metallic-roughness texture. */
	MetallicRoughnessMap = "metallicRoughnessTexture",
	/** Metallic-roughness texture scale and offset as a `vec4`. */
	MetallicRoughnessMapScaleOffset = "metallicRoughnessTexture_ST",
	/** Metallic-roughness texture rotation as a non-negative number in `[0, +inf]`. */
	MetallicRoughnessMapRotation = "metallicRoughnessTextureRotation",
	/** Alpha cutoff value in the range `[0.0, 1.0]`. */
	AlphaCutoff = "alphaCutoff",
	/** Culling mode, where `0` means two-sided and `2` means front-only. */
	Culling = "_Cull"
}
/**
 * Supported alpha modes for a material.
 */
export declare enum AlphaMode {
	/** Fully opaque alpha mode. */
	Opaque = "Opaque",
	/** Masked alpha mode that uses the alpha cutoff to discard low-alpha fragments. */
	Mask = "Mask",
	/** Blended alpha mode that mixes the material color with other colors using alpha. */
	Blend = "Blend"
}
/**
 * Creates and updates materials.
 */
export declare class MaterialManager {
	/**
	 * Creates a material.
	 *
	 * @returns A promise that resolves to the created {@link Material}.
	 */
	static Create(): Promise<Material>;
	/**
	 * Creates a material and assigns it to an entity.
	 *
	 * @param entity - The {@link Entity} to assign the material to.
	 * @param submeshIndex - The submesh index to assign the material to. Defaults
	 * to `0`.
	 * @returns A promise that resolves to the created {@link Material}.
	 */
	static Create(entity: Entity, submeshIndex?: number): Promise<Material>;
	/**
	 * Destroys a material.
	 *
	 * @param handle - The {@link Material} to destroy.
	 * @returns A promise that resolves to `true` if the material was destroyed, or
	 * `false` otherwise.
	 */
	static Destroy(handle: Material): Promise<boolean>;
	/**
	 * Sets a numeric property on a material.
	 *
	 * @param handle - The {@link Material} to update.
	 * @param property - The property to change.
	 * @param value - The numeric value to assign.
	 * @returns A promise that resolves when the property has been changed.
	 */
	static SetFloat(handle: Material, property: MaterialProperty, value: number): Promise<void>;
	/**
	 * Gets a numeric property from a material.
	 *
	 * @param handle - The {@link Material} to inspect.
	 * @param property - The property to read.
	 * @returns A promise that resolves to the numeric property value.
	 */
	static GetFloat(handle: Material, property: MaterialProperty): Promise<number>;
	/**
	 * Sets a vector property on a material.
	 *
	 * @param handle - The {@link Material} to update.
	 * @param property - The property to change.
	 * @param value - The vector value to assign.
	 * @returns A promise that resolves when the property has been changed.
	 */
	static SetVector(handle: Material, property: MaterialProperty, value: vec4): Promise<void>;
	/**
	 * Gets a vector property from a material.
	 *
	 * @param handle - The {@link Material} to inspect.
	 * @param property - The property to read.
	 * @returns A promise that resolves to the vector property value.
	 */
	static GetVector(handle: Material, property: MaterialProperty): Promise<vec4>;
	/**
	 * Sets a color property on a material.
	 *
	 * @param handle - The {@link Material} to update.
	 * @param property - The property to change.
	 * @param rgba - The color value to assign as an RGBA `vec4`, with each channel
	 * in the range `[0.0, 1.0]`. For HDR emission, values greater than `1.0` are
	 * supported.
	 * @returns A promise that resolves when the property has been changed.
	 */
	static SetColor(handle: Material, property: MaterialProperty, rgba: vec4): Promise<void>;
	/**
	 * Gets a color property from a material.
	 *
	 * @param handle - The {@link Material} to inspect.
	 * @param property - The property to read.
	 * @returns A promise that resolves to the color value as an RGBA `vec4`, with
	 * each channel in the range `[0.0, 1.0]`.
	 */
	static GetColor(handle: Material, property: MaterialProperty): Promise<vec4>;
	/**
	 * Sets a texture property on a material.
	 *
	 * @param handle - The {@link Material} to update.
	 * @param property - The property to change.
	 * @param texture - The {@link Texture} to assign.
	 * @returns A promise that resolves when the property has been changed.
	 */
	static SetTexture(handle: Material, property: MaterialProperty, texture: Texture): Promise<void>;
	/**
	 * Gets a texture property from a material.
	 *
	 * @param handle - The {@link Material} to inspect.
	 * @param property - The property to read.
	 * @returns A promise that resolves to the assigned {@link Texture}.
	 */
	static GetTexture(handle: Material, property: MaterialProperty): Promise<number>;
	/**
	 * Sets the alpha mode of a material.
	 *
	 * @param handle - The {@link Material} to update.
	 * @param alphaMode - The alpha mode to assign. Defaults to
	 * {@link AlphaMode.Opaque}.
	 * @returns A promise that resolves when the alpha mode has been changed.
	 */
	static SetAlphaMode(handle: Material, alphaMode?: AlphaMode): Promise<void>;
	/**
	 * Gets the alpha mode of a material.
	 *
	 * @param handle - The {@link Material} to inspect.
	 * @returns A promise that resolves to the alpha mode.
	 */
	static GetAlphaMode(handle: Material): Promise<AlphaMode>;
}
/**
 * Opaque numeric handle that identifies a mesh.
 */
export type Mesh = number;
/**
 * Creates and updates meshes.
 */
export declare class MeshManager {
	/**
	 * Creates a mesh.
	 *
	 * @returns A promise that resolves to the created {@link Mesh}.
	 */
	static Create(): Promise<Mesh>;
	/**
	 * Creates a mesh and assigns it to an entity.
	 *
	 * @param entity - The {@link Entity} to assign the mesh to.
	 * @returns A promise that resolves to the created {@link Mesh}.
	 */
	static Create(entity: Entity): Promise<Mesh>;
	/**
	 * Destroys a mesh.
	 *
	 * @param handle - The {@link Mesh} to destroy.
	 * @returns A promise that resolves to `true` if the mesh was destroyed, or
	 * `false` otherwise.
	 */
	static Destroy(handle: Mesh): Promise<boolean>;
	/**
	 * Sets the vertex positions of a mesh.
	 * Typed arrays are recommended for best performance.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @param vertices - Vertex positions.
	 * @returns A promise that resolves when the vertex positions have been changed.
	 */
	static SetVertices(handle: Mesh, vertices: Float32Array): Promise<void>;
	/**
	 * Sets the vertex positions of a mesh.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @param base64Vertices - Base64-encoded vertex positions.
	 * @returns A promise that resolves when the vertex positions have been changed.
	 */
	static SetVertices(handle: Mesh, base64Vertices: string): Promise<void>;
	/**
	 * Sets the triangle indices of a mesh.
	 * Typed arrays are recommended for best performance.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @param indices - Triangle indices.
	 * @returns A promise that resolves when the triangle indices have been changed.
	 */
	static SetTriangles(handle: Mesh, indices: Uint16Array): Promise<void>;
	/**
	 * Sets the triangle indices of a mesh.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @param base64Indices - Base64-encoded triangle indices.
	 * @returns A promise that resolves when the triangle indices have been changed.
	 */
	static SetTriangles(handle: Mesh, base64Indices: string): Promise<void>;
	/**
	 * Sets the UV coordinates of a mesh.
	 * Typed arrays are recommended for best performance.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @param uvs - UV coordinates.
	 * @returns A promise that resolves when the UV coordinates have been changed.
	 */
	static SetUVs(handle: Mesh, uvs: Float32Array): Promise<void>;
	/**
	 * Sets the UV coordinates of a mesh.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @param base64Uvs - Base64-encoded UV coordinates.
	 * @returns A promise that resolves when the UV coordinates have been changed.
	 */
	static SetUVs(handle: Mesh, base64Uvs: string): Promise<void>;
	/**
	 * Sets the normals of a mesh.
	 * Typed arrays are recommended for best performance.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @param normals - Normal vectors.
	 * @returns A promise that resolves when the normals have been changed.
	 */
	static SetNormals(handle: Mesh, normals: Float32Array): Promise<void>;
	/**
	 * Sets the normals of a mesh.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @param base64Normals - Base64-encoded normal vectors.
	 * @returns A promise that resolves when the normals have been changed.
	 */
	static SetNormals(handle: Mesh, base64Normals: string): Promise<void>;
	/**
	 * Recalculates the normals of a mesh.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @returns A promise that resolves when the normals have been recalculated.
	 */
	static RecalcNormals(handle: Mesh): Promise<void>;
	/**
	 * Recalculates the bounds of a mesh.
	 *
	 * @param handle - The {@link Mesh} to update.
	 * @returns A promise that resolves when the bounds have been recalculated.
	 */
	static RecalcBounds(handle: Mesh): Promise<void>;
	/**
	 * Gets the number of blend shapes in a mesh.
	 *
	 * @param handle - The {@link Mesh} to inspect.
	 * @returns A promise that resolves to the number of blend shapes. Throw if
	 * the mesh does not exist.
	 */
	static BlendShapeCount(handle: Mesh): Promise<number>;
}
/**
 * Supported shadow casting modes for a renderable component.
 */
export declare enum ShadowCastingMode {
	/** No shadows are cast from this object. */
	Off = 0,
	/** Shadows are cast from this object. */
	On = 1,
	/** Shadows are cast from this object, treating it as two-sided. */
	TwoSided = 2,
	/** Object casts shadows, but is otherwise invisible in the Scene. */
	ShadowsOnly = 3
}
/**
 * Creates and updates renderable components.
 */
export declare class RenderableManager {
	/**
	 * Creates a renderable component on an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param isSkinnedMesh - Whether the renderable should be created as a skinned
	 * mesh. Defaults to `false`. Blend shapes only work when this is `true`.
	 * @returns A promise that resolves to `true` if the renderable component was
	 * created, or `false` otherwise.
	 */
	static Create(entity: Entity, isSkinnedMesh?: boolean): Promise<boolean>;
	/**
	 * Removes a renderable component from an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the renderable component was
	 * removed, or `false` otherwise.
	 */
	static Destroy(entity: Entity): Promise<boolean>;
	/**
	 * Checks whether an entity has a renderable component.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the entity has a renderable
	 * component, or `false` otherwise.
	 */
	static HasComponent(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a renderable component is enabled.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param enabled - Whether the renderable component is enabled.
	 * @returns A promise that resolves when the enabled state has been changed.
	 */
	static SetEnabled(entity: Entity, enabled: boolean): Promise<void>;
	/**
	 * Gets whether a renderable component is enabled.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the renderable component is
	 * enabled, or `false` otherwise.
	 */
	static GetEnabled(entity: Entity): Promise<boolean>;
	/**
	 * Sets the mesh of a renderable component.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param mesh - The {@link Mesh} to assign.
	 * @returns A promise that resolves when the mesh has been changed.
	 */
	static SetMesh(entity: Entity, mesh: Mesh): Promise<void>;
	/**
	 * Gets the mesh of a renderable component.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the assigned {@link Mesh}.
	 * @throws An error if no mesh is assigned to the renderable component.
	 */
	static GetMesh(entity: Entity): Promise<number>;
	/**
	 * Sets the weight of a blend shape.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param index - The blend shape index.
	 * @param weight - The blend shape weight in the range `[0.0, 1.0]`.
	 * @returns A promise that resolves when the blend shape weight has been
	 * changed.
	 */
	static SetBlendShapeWeight(entity: Entity, index: number, weight: number): Promise<void>;
	/**
	 * Gets the weight of a blend shape.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @param index - The blend shape index.
	 * @returns A promise that resolves to the blend shape weight.
	 */
	static GetBlendShapeWeight(entity: Entity, index: number): Promise<number>;
	/**
	 * Sets the material of a renderable component.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param material - The {@link Material} to assign.
	 * @param index - The material index. Defaults to `0`.
	 * @returns A promise that resolves when the material has been changed.
	 */
	static SetMaterial(entity: Entity, material: Material, index?: number): Promise<void>;
	/**
	 * Gets the material of a renderable component.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @param index - The material index. Defaults to `0`.
	 * @returns A promise that resolves to the assigned {@link Material}.
	 * @throws An error if no material is assigned to the renderable component at the given index.
	 */
	static GetMaterial(entity: Entity, index?: number): Promise<number>;
	/**
	 * Sets whether a renderable component receives shadows.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param receive - Whether the renderable component receives shadows.
	 * @returns A promise that resolves when the receive-shadows setting has been
	 * changed.
	 */
	static SetReceiveShadows(entity: Entity, receive: boolean): Promise<void>;
	/**
	 * Gets whether a renderable component receives shadows.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the renderable component
	 * receives shadows, or `false` otherwise.
	 */
	static GetReceiveShadows(entity: Entity): Promise<boolean>;
	/**
	 * Sets the shadow casting mode of a renderable component.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param shadowMode - The shadow casting mode to assign.
	 * @returns A promise that resolves when the shadow casting mode has been
	 * changed.
	 */
	static SetShadowMode(entity: Entity, shadowMode: ShadowCastingMode): Promise<void>;
	/**
	 * Gets the shadow casting mode of a renderable component.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the shadow casting mode.
	 */
	static GetShadowMode(entity: Entity): Promise<ShadowCastingMode>;
}
/**
 * Supported movement types for grab interactions.
 */
export declare enum MovementType {
	/** Velocity-tracked movement. */
	VelocityTracking = 0,
	/** Kinematic movement. */
	Kinematic = 1,
	/** Instantaneous movement. */
	Instantaneous = 2
}
/**
 * Creates and updates grab interactable components.
 */
export declare class GrabInteractableManager {
	/**
	 * Creates a grab interactable component on an entity.
	 *
	 * Colliders added before the component is created are used for interaction
	 * detection. Interaction states include hover, select, and activate.
	 * Hover begins when a user's hand touches or points at the
	 * collider, select begins when the user presses the grip input to grab the
	 * entity, and activate begins when the user presses the trigger input.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the grab interactable
	 * component was created, or `false` otherwise.
	 */
	static Create(entity: Entity): Promise<boolean>;
	/**
	 * Makes a grab interactable network-aware.
	 *
	 * The grab interactable can then be used by network users, and its transform
	 * is synchronized across the network session. An error is thrown in local mode
	 * or when the entity does not use a network transform.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves when network grab behavior has been
	 * configured.
	 */
	static MakeNetworkGrabble(entity: Entity): Promise<void>;
	/**
	 * Removes a grab interactable component from an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the component was removed, or
	 * `false` otherwise.
	 */
	static Destroy(entity: Entity): Promise<boolean>;
	/**
	 * Checks whether an entity has a grab interactable component.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the component exists, or
	 * `false` otherwise.
	 */
	static HasComponent(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a grab interactable component is enabled.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param enabled - Whether the grab interactable component is enabled.
	 * @returns A promise that resolves when the enabled state has been changed.
	 */
	static SetEnabled(entity: Entity, enabled: boolean): Promise<void>;
	/**
	 * Gets whether a grab interactable component is enabled.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the component is enabled, or
	 * `false` otherwise.
	 */
	static GetEnabled(entity: Entity): Promise<boolean>;
	/**
	 * Gets whether the grab interactable tracks position.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if position tracking is enabled,
	 * or `false` otherwise.
	 */
	static GetTrackPosition(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a grab interactable tracks position.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param isTracking - Whether position tracking is enabled.
	 * @returns A promise that resolves when the setting has been changed.
	 */
	static SetTrackPosition(entity: Entity, isTracking: boolean): Promise<void>;
	/**
	 * Gets whether the grab interactable tracks rotation.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if rotation tracking is enabled,
	 * or `false` otherwise.
	 */
	static GetTrackRotation(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a grab interactable tracks rotation.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param isTracking - Whether rotation tracking is enabled.
	 * @returns A promise that resolves when the setting has been changed.
	 */
	static SetTrackRotation(entity: Entity, isTracking: boolean): Promise<void>;
	/**
	 * Gets whether a grab interactable applies physics-based throwing on detach.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if throw-on-detach is enabled, or
	 * `false` otherwise.
	 */
	static GetThrowOnDetach(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a grab interactable applies physics-based throwing on detach.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param enable - Whether throw-on-detach is enabled.
	 * @returns A promise that resolves when the setting has been changed.
	 */
	static SetThrowOnDetach(entity: Entity, enable: boolean): Promise<void>;
	/**
	 * Gets the attach entity of a grab interactable.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the attached {@link Entity}.
	 */
	static GetAttachEntity(entity: Entity): Promise<number>;
	/**
	 * Sets the attach entity of a grab interactable.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param attachEntity - The target {@link Entity} to attach to.
	 * @returns A promise that resolves when the attach entity has been changed.
	 */
	static SetAttachEntity(entity: Entity, attachEntity: Entity): Promise<void>;
	/**
	 * Sets whether hover activation is allowed.
	 *
	 * When enabled, the object can be activated with the trigger input while it is
	 * hovered, without being selected first.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param allowHoverActivate - Whether hover activation is allowed.
	 * @returns A promise that resolves when the setting has been changed.
	 */
	static SetAllowHoverActivate(entity: Entity, allowHoverActivate: boolean): Promise<void>;
	/**
	 * Gets whether hover activation is allowed.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if hover activation is allowed,
	 * or `false` otherwise.
	 */
	static GetAllowHoverActivate(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether dynamic attach is enabled.
	 *
	 * When enabled, the object can be grabbed directly at the collider contact
	 * point, and the attach anchor does not move to the hand transform.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param dynamicAttach - Whether dynamic attach is enabled.
	 * @returns A promise that resolves when the setting has been changed.
	 */
	static SetDynamicAttach(entity: Entity, dynamicAttach: boolean): Promise<void>;
	/**
	 * Gets whether dynamic attach is enabled.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if dynamic attach is enabled, or
	 * `false` otherwise.
	 */
	static GetDynamicAttach(entity: Entity): Promise<boolean>;
	/**
	 * Sets the movement type of a grab interactable.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param movementType - The movement type to assign.
	 * @returns A promise that resolves when the movement type has been changed.
	 */
	static SetMovementType(entity: Entity, movementType: MovementType): Promise<void>;
	/**
	 * Gets the movement type of a grab interactable.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the movement type.
	 */
	static GetMovementType(entity: Entity): Promise<MovementType>;
	/**
	 * Cancels the current select state of a grab interactable.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves when the select state has been canceled.
	 */
	static CancelSelect(entity: Entity): Promise<void>;
	/**
	 * Registers a callback for hover enter events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onHoverEntered - The callback invoked when hover begins.
	 * @returns A promise that resolves when the callback has been registered.
	 */
	static AddHoverEnteredCallback(entity: Entity, onHoverEntered: (interactableEntity: Entity, interactorEntity: Entity) => void): Promise<void>;
	/**
	 * Registers a callback for hover exit events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onHoverExited - The callback invoked when hover ends.
	 * @returns A promise that resolves when the callback has been registered.
	 */
	static AddHoverExitedCallback(entity: Entity, onHoverExited: (interactableEntity: Entity, interactorEntity: Entity) => void): Promise<void>;
	/**
	 * Registers a callback for select enter events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onSelectEntered - The callback invoked when selection begins.
	 * @returns A promise that resolves when the callback has been registered.
	 */
	static AddSelectEnteredCallback(entity: Entity, onSelectEntered: (interactableEntity: Entity, interactorEntity: Entity) => void): Promise<void>;
	/**
	 * Registers a callback for select exit events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onSelectExited - The callback invoked when selection ends.
	 * @returns A promise that resolves when the callback has been registered.
	 */
	static AddSelectExitedCallback(entity: Entity, onSelectExited: (interactableEntity: Entity, interactorEntity: Entity) => void): Promise<void>;
	/**
	 * Registers a callback for activate events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onActivated - The callback invoked when activation begins.
	 * @returns A promise that resolves when the callback has been registered.
	 */
	static AddActivatedCallback(entity: Entity, onActivated: (interactableEntity: Entity, interactorEntity: Entity) => void): Promise<void>;
	/**
	 * Registers a callback for deactivate events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onDeactivated - The callback invoked when activation ends.
	 * @returns A promise that resolves when the callback has been registered.
	 */
	static AddDeactivatedCallback(entity: Entity, onDeactivated: (interactableEntity: Entity, interactorEntity: Entity) => void): Promise<void>;
}
/**
 * Supported camera projection types.
 */
export declare enum ProjectionType {
	/** Perspective projection. */
	Perspective = 0,
	/** Orthographic projection. */
	Orthographic = 1
}
/**
 * Creates and updates camera components.
 */
export declare class CameraManager {
	/**
	 * Creates a camera component on an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the camera component was
	 * created, or `false` otherwise.
	 */
	static Create(entity: Entity): Promise<boolean>;
	/**
	 * Removes a camera component from an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the camera component was
	 * removed, or `false` otherwise.
	 */
	static Destroy(entity: Entity): Promise<boolean>;
	/**
	 * Checks whether an entity has a camera component.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the entity has a camera
	 * component, or `false` otherwise.
	 */
	static HasComponent(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a camera component is enabled.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param enabled - Whether the camera component is enabled.
	 * @returns A promise that resolves when the enabled state has been changed.
	 */
	static SetEnabled(entity: Entity, enabled: boolean): Promise<void>;
	/**
	 * Gets whether a camera component is enabled.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the camera component is
	 * enabled, or `false` otherwise.
	 */
	static GetEnabled(entity: Entity): Promise<boolean>;
	/**
	 * Sets the render texture of a camera.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param renderTexture - The {@link Texture} to assign as the render texture;
	 * it must be a render texture.
	 * @returns A promise that resolves when the render texture has been changed.
	 */
	static SetRenderTexture(entity: Entity, renderTexture: Texture): Promise<void>;
	/**
	 * Gets the render texture of a camera.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the assigned {@link Texture}.
	 */
	static GetRenderTexture(entity: Entity): Promise<number>;
	/**
	 * Sets the projection type of a camera.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param projectionType - The projection type to assign.
	 * @returns A promise that resolves when the projection type has been changed.
	 */
	static SetProjectionType(entity: Entity, projectionType: ProjectionType): Promise<void>;
	/**
	 * Gets the projection type of a camera.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the projection type.
	 */
	static GetProjectionType(entity: Entity): Promise<ProjectionType>;
	/**
	 * Sets the field of view of a camera in degrees.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param fov - The field of view in degrees.
	 * @returns A promise that resolves when the field of view has been changed.
	 */
	static SetFieldOfView(entity: Entity, fov: number): Promise<void>;
	/**
	 * Gets the field of view of a camera in degrees.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the field of view in degrees.
	 */
	static GetFieldOfView(entity: Entity): Promise<number>;
	/**
	 * Sets the orthographic size of a camera.
	 *
	 * This value is used when the projection type is
	 * {@link ProjectionType.Orthographic}.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param orthographicSize - The orthographic size.
	 * @returns A promise that resolves when the orthographic size has been
	 * changed.
	 */
	static SetOrthographicSize(entity: Entity, orthographicSize: number): Promise<void>;
	/**
	 * Gets the orthographic size of a camera.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the orthographic size.
	 */
	static GetOrthographicSize(entity: Entity): Promise<number>;
	/**
	 * Sets the near clip plane distance of a camera.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param nearClipPlane - The near clip plane distance.
	 * @returns A promise that resolves when the near clip plane has been changed.
	 */
	static SetNearClipPlane(entity: Entity, nearClipPlane: number): Promise<void>;
	/**
	 * Gets the near clip plane distance of a camera.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the near clip plane distance.
	 */
	static GetNearClipPlane(entity: Entity): Promise<number>;
	/**
	 * Sets the far clip plane distance of a camera.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param farClipPlane - The far clip plane distance.
	 * @returns A promise that resolves when the far clip plane has been changed.
	 */
	static SetFarClipPlane(entity: Entity, farClipPlane: number): Promise<void>;
	/**
	 * Gets the far clip plane distance of a camera.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the far clip plane distance.
	 */
	static GetFarClipPlane(entity: Entity): Promise<number>;
}
/**
 * Supported shadow modes for a light.
 */
export declare enum LightShadowMode {
	/** Shadow casting is disabled. */
	NoShadows = 0,
	/** Cast hard shadows. */
	Hard = 1,
	/** Cast soft shadows. */
	Soft = 2
}
/**
 * Supported light types.
 */
export declare enum LightType {
	/** Spot light. */
	Spot = 0,
	/** Directional light. */
	Directional = 1,
	/** Point light. */
	Point = 2
}
/**
 * Creates and updates light components.
 */
export declare class LightManager {
	/**
	 * Creates a light component on an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param type - The light type to create.
	 * @returns A promise that resolves to `true` if the light component was
	 * created, or `false` otherwise.
	 */
	static Create(entity: Entity, type: LightType): Promise<boolean>;
	/**
	 * Removes a light component from an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the light component was
	 * removed, or `false` otherwise.
	 */
	static Destroy(entity: Entity): Promise<boolean>;
	/**
	 * Checks whether an entity has a light component.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the entity has a light
	 * component, or `false` otherwise.
	 */
	static HasComponent(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a light component is enabled.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param enabled - Whether the light component is enabled.
	 * @returns A promise that resolves when the enabled state has been changed.
	 */
	static SetEnabled(entity: Entity, enabled: boolean): Promise<void>;
	/**
	 * Gets whether a light component is enabled.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the light component is
	 * enabled, or `false` otherwise.
	 */
	static GetEnabled(entity: Entity): Promise<boolean>;
	/**
	 * Sets the light type.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param type - The light type to assign.
	 * @returns A promise that resolves when the light type has been changed.
	 */
	static SetType(entity: Entity, type: LightType): Promise<void>;
	/**
	 * Gets the light type.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the light type.
	 */
	static GetType(entity: Entity): Promise<LightType>;
	/**
	 * Sets the light color.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param rgb - The RGB color as a `vec3`, with each channel in the range
	 * `[0.0, 1.0]`.
	 * @returns A promise that resolves when the color has been changed.
	 */
	static SetColor(entity: Entity, rgb: vec3): Promise<void>;
	/**
	 * Gets the light color.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the RGB color as a `vec3`, with each
	 * channel in the range `[0.0, 1.0]`.
	 */
	static GetColor(entity: Entity): Promise<vec3>;
	/**
	 * Sets the light intensity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param intensity - The light intensity.
	 * @returns A promise that resolves when the intensity has been changed.
	 */
	static SetIntensity(entity: Entity, intensity: number): Promise<void>;
	/**
	 * Gets the light intensity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the light intensity.
	 */
	static GetIntensity(entity: Entity): Promise<number>;
	/**
	 * Sets the light range.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param range - The light range.
	 * @returns A promise that resolves when the range has been changed.
	 */
	static SetRange(entity: Entity, range: number): Promise<void>;
	/**
	 * Gets the light range.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the light range.
	 */
	static GetRange(entity: Entity): Promise<number>;
	/**
	 * Sets the spot angle in degrees.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param angle - The spot angle in degrees.
	 * @returns A promise that resolves when the spot angle has been changed.
	 */
	static SetSpotAngle(entity: Entity, angle: number): Promise<void>;
	/**
	 * Gets the spot angle in degrees.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the spot angle in degrees.
	 */
	static GetSpotAngle(entity: Entity): Promise<number>;
	/**
	 * Sets the shadow mode of a light.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param mode - The shadow mode to assign.
	 * @returns A promise that resolves when the shadow mode has been changed.
	 */
	static SetShadows(entity: Entity, mode: LightShadowMode): Promise<void>;
	/**
	 * Gets the shadow mode of a light.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the shadow mode.
	 */
	static GetShadows(entity: Entity): Promise<LightShadowMode>;
}
/**
 * Supported asset types.
 */
export declare enum AssetType {
	/** Material asset. */
	Material = "Material",
	/** Texture asset. */
	Texture = "Texture",
	/** Mesh asset. */
	Mesh = "Mesh",
	/** Prefab asset. */
	Prefab = "Prefab"
}
/**
 * Supported component types.
 */
export declare enum ComponentType {
	/** Property component. */
	Property = "Property",
	/** Transform component. */
	Transform = "Transform",
	/** Renderable component. */
	Renderable = "Renderable",
	/** Camera component. */
	Camera = "Camera",
	/** Light component. */
	Light = "Light",
	/** Rigidbody component. */
	Rigidbody = "Rigid Body",
	/** Collider component. */
	Collider = "Collider",
	/** Grabble component. */
	Grabble = "Grabble"
}
/**
 * Texture transform data.
 */
export interface TextureTransform {
	/** Texture offset. */
	offset: vec2;
	/** Texture scale. */
	scale: vec2;
	/** Texture rotation. */
	rotation: number;
}
/**
 * Sparse morph target data.
 */
export interface SparseMorphData {
	/** Total array size. */
	arraySize: number;
	/** Indices of sparse entries. */
	sparseIndices: Uint16Array;
	/** Values of sparse entries. */
	sparseValues: Float32Array;
}
/**
 * Mesh primitive data.
 */
export interface MeshPrimitive {
	/** Triangle indices. */
	indices: Uint16Array;
	/** Vertex positions. */
	vertices: Float32Array;
	/** Vertex normals. */
	normals?: Float32Array;
	/** Vertex UV coordinates. */
	uvs?: Float32Array;
	/** Morph target position data. */
	morphPosition?: (Float32Array | SparseMorphData)[];
	/** Morph target normal data. */
	morphNormal?: (Float32Array | SparseMorphData)[];
}
/**
 * Base asset data.
 */
export interface Asset {
	/** Asset name. */
	name: string;
	/** Asset UUID. */
	uuid: UUID;
	/** Asset type. */
	assetType: AssetType;
}
/**
 * Texture asset data.
 */
export interface TextureAsset extends Asset {
	/** Encoded image data. */
	image: Uint8Array;
	/** Image MIME type. */
	mimeType: string;
	/** Texture filter mode. */
	filterMode: TextureFilterMode;
	/** Texture wrap mode on the U axis. */
	wrapModeU: TextureWrapMode;
	/** Texture wrap mode on the V axis. */
	wrapModeV: TextureWrapMode;
}
/**
 * Material asset data.
 */
export interface MaterialAsset extends Asset {
	/** Base color as an RGBA `vec4`. */
	baseColor: vec4;
	/** Base color texture UUID. */
	baseColorMap?: UUID;
	/** Base color texture transform. */
	baseColorTransform: TextureTransform;
	/** Normal scale. */
	normalScale: number;
	/** Normal texture UUID. */
	normalMap?: UUID;
	/** Normal texture transform. */
	normalTransform: TextureTransform;
	/** Emission color as an RGB `vec3`. */
	emission: vec3;
	/** Emission intensity. */
	emissionIntensity: number;
	/** Emission texture UUID. */
	emissionMap?: UUID;
	/** Emission texture transform. */
	emissionTransform: TextureTransform;
	/** Metalness value. */
	metalness: number;
	/** Roughness value. */
	roughness: number;
	/** Metallic-roughness texture UUID. */
	metallicRoughnessMap?: UUID;
	/** Metallic-roughness texture transform. */
	metallicRoughnessTransform: TextureTransform;
	/** Occlusion strength. */
	occlusionStrength: number;
	/** Occlusion texture UUID. */
	occlusionMap?: UUID;
	/** Occlusion texture transform. */
	occlusionTransform: TextureTransform;
	/** Alpha mode. */
	alphaMode: AlphaMode;
	/** Alpha cutoff value. */
	alphaCutoff: number;
}
/**
 * Mesh asset data.
 */
export interface MeshAsset extends Asset {
	/** Mesh primitives. */
	meshPrimitives: MeshPrimitive[];
	/** Morph target names. */
	morphTargets?: string[];
	/** Morph target weights. */
	morphWeights?: number[];
}
/**
 * Base component data.
 */
export interface Component {
	/** Component type. */
	componentType: ComponentType;
	/** Whether the component is enabled. */
	enabled: boolean;
}
/**
 * Property component data.
 */
export interface PropertyComponent extends Component {
	/** Entity name. */
	name: string;
	/** Entity layer. */
	layer: number;
	/** Whether the entity is active. */
	activeEntity: boolean;
}
/**
 * Transform component data.
 */
export interface TransformComponent extends Component {
	/** Parent entity UUID. */
	parent: UUID;
	/** Child entity UUIDs. */
	children: UUID[];
	/** Local position. */
	localPosition: vec3;
	/** Local rotation. */
	localRotation: vec3;
	/** Local scale. */
	localScale: vec3;
	/** Whether transform sync is enabled. */
	isTransformSync: boolean;
}
/**
 * Renderable component data.
 */
export interface RenderableComponent extends Component {
	/** Mesh UUID. */
	mesh?: UUID;
	/** Material UUIDs by slot. */
	materialList: (UUID | undefined)[];
	/** Whether the renderable receives shadows. */
	receiveShadows: boolean;
	/** Shadow casting mode. */
	castShadows: ShadowCastingMode;
	/** Whether culling is enabled. */
	culling: boolean;
}
/**
 * Grabble component data.
 */
export interface GrabbleComponent extends Component {
	/** Movement type. */
	movementType: MovementType;
	/** Whether dynamic attach is enabled. */
	dynamicAttach: boolean;
	/** Attach entity UUID. */
	attachEntity?: UUID;
	/** Whether hover activate is allowed. */
	allowHoverActivate: boolean;
	/** Whether position tracking is enabled. */
	trackPosition: boolean;
	/** Whether rotation tracking is enabled. */
	trackRotation: boolean;
	/** Whether throw-on-detach is enabled. */
	throwOnDetach: boolean;
}
/**
 * Rigidbody component data.
 */
export interface RigidbodyComponent extends Component {
	/** Whether the rigidbody is kinematic. */
	isKinematic: boolean;
	/** Angular damping. */
	angularDamping: number;
	/** Linear damping. */
	linearDamping: number;
	/** Mass. */
	mass: number;
	/** Whether gravity is enabled. */
	useGravity: boolean;
}
/**
 * Camera component data.
 */
export interface CameraComponent extends Component {
	/** Projection type. */
	projectionType: ProjectionType;
	/** Perspective field of view. */
	perspectiveFov: number;
	/** Orthographic size. */
	orthographicSize: number;
	/** Near clipping plane distance. */
	clippingNear: number;
	/** Far clipping plane distance. */
	clippingFar: number;
	/** Camera culling mask. */
	cullingMask: number;
}
/**
 * Light component data.
 */
export interface LightComponent extends Component {
	/** Light type. */
	lightType: LightType;
	/** Light color. */
	color: vec3;
	/** Light intensity. */
	intensity: number;
	/** Light range. */
	range: number;
	/** Spot lights angle in degree. Only valid for spot lights. */
	spotAngle: number;
	/** Shadow mode. */
	shadows: LightShadowMode;
	/** Light culling mask. */
	cullingMask: number;
}
/**
 * Supported collider types.
 */
export declare enum ColliderType {
	/** Box collider. */
	Box = "Box",
	/** Capsule collider. */
	Capsule = "Capsule",
	/** Sphere collider. */
	Sphere = "Sphere"
}
/**
 * Base collider data.
 */
export interface ColliderBase {
	/** Whether the collider is a trigger. */
	isTrigger: boolean;
	/** Collider type. */
	colliderType: ColliderType;
	/** Collider center. */
	center: vec3;
}
/**
 * Box collider data.
 */
export interface BoxCollider extends ColliderBase {
	/** Box size. */
	size: vec3;
}
/**
 * Sphere collider data.
 */
export interface SphereCollider extends ColliderBase {
	/** Sphere radius. */
	radius: number;
}
/**
 * Capsule collider data.
 */
export interface CapsuleCollider extends ColliderBase {
	/** Capsule height. */
	height: number;
	/** Capsule radius. */
	radius: number;
}
/**
 * Collider component data.
 */
export interface ColliderComponent extends Component {
	/** Collider list. */
	colliders: (CapsuleCollider | SphereCollider | BoxCollider)[];
}
/**
 * Prefab asset data.
 */
export interface PrefabAsset extends Asset {
	/** Root entity UUID. */
	rootEntity?: UUID;
	/** Entity UUIDs. */
	entities: Set<UUID>;
	/** Property components by entity UUID. */
	properties: Map<UUID, PropertyComponent>;
	/** Transform components by entity UUID. */
	transforms: Map<UUID, TransformComponent>;
	/** Renderable components by entity UUID. */
	renderables: Map<UUID, RenderableComponent>;
	/** Grabble components by entity UUID. */
	grabbles: Map<UUID, GrabbleComponent>;
	/** Rigidbody components by entity UUID. */
	rigidbodies: Map<UUID, RigidbodyComponent>;
	/** Light components by entity UUID. */
	lights: Map<UUID, LightComponent>;
	/** Camera components by entity UUID. */
	cameras: Map<UUID, CameraComponent>;
	/** Collider components by entity UUID. */
	colliders: Map<UUID, ColliderComponent>;
}
/**
 * World-level project settings.
 * These properties are used only during a world setup
 */
export interface WorldProperty {
	/** Tracked entity UUID. */
	trackedEntity?: UUID;
	/** Whether the world has an entrance. */
	worldEntrance: boolean;
	/** Spawn rotation. */
	spawnRotation: vec3;
	/** Spawn position. */
	spawnPosition: vec3;
	/** Skybox texture. */
	skyboxTexture?: UUID;
	/** Environment intensity. */
	environmentIntensity: number;
}
/**
 * Semantic version string.
 */
export type Version = `${number}.${number}.${number}`;
/**
 * Project metadata.
 */
export interface ProjectMetadata {
	/** Project name. */
	name: string;
	/** Project author. */
	author: string;
	/** Project version. */
	version: Version;
	/** Preview image path. */
	previewImagePath?: string;
}
/**
 * Project description data.
 */
export interface ProjectDescription {
	/** Project metadata. */
	metadata: ProjectMetadata;
	/** World settings. */
	world: WorldProperty;
	/** Scene data. */
	scene: {
		/** Entity UUIDs. */
		entities: Set<UUID>;
		/** Property components by entity UUID. */
		properties: Map<UUID, PropertyComponent>;
		/** Transform components by entity UUID. */
		transforms: Map<UUID, TransformComponent>;
		/** Renderable components by entity UUID. */
		renderables: Map<UUID, RenderableComponent>;
		/** Grabble components by entity UUID. */
		grabbles: Map<UUID, GrabbleComponent>;
		/** Rigidbody components by entity UUID. */
		rigidbodies: Map<UUID, RigidbodyComponent>;
		/** Light components by entity UUID. */
		lights: Map<UUID, LightComponent>;
		/** Camera components by entity UUID. */
		cameras: Map<UUID, CameraComponent>;
		/** Collider components by entity UUID. */
		colliders: Map<UUID, ColliderComponent>;
	};
}
/**
 * Project bundle data.
 */
export interface ProjectBundle {
	/** Project description. */
	project: ProjectDescription;
	/** Assets by UUID. */
	assets: Map<UUID, Asset>;
}
export interface SceneGraph {
}
/** Adamas Runtime API Version */
export declare const API_VERSION: Version;
/**
 * Represents a project.
 */
export declare class Project {
	private static projectId;
	private readonly bundle?;
	private readonly metadata;
	private constructor();
	/**
	 * Gets the current project ID assigned by the runtime.
	 *
	 * This ID corresponds to the project identifier assigned after the project is
	 * uploaded to the cloud. If the project has not been uploaded, the runtime
	 * generates a local identifier instead.
	 *
	 * @returns The current project ID.
	 * @throws If the project ID has not been initialized by the runtime.
	 */
	static GetProjectId(): string;
	/**
	 * Creates a project from a project bundle.
	 *
	 * @param bundle - The project bundle.
	 * @returns The created {@link Project}.
	 */
	static FromBundle(bundle: ProjectBundle): Project;
	/**
	 * Creates a new project from project metadata. This is intended for projects distributed outside the platform.
	 *
	 * @param metadata - The project metadata, excluding the project identifier, which is generated at runtime.
	 * @returns The created {@link Project}.
	 */
	static New(metadata: ProjectMetadata): Project;
	/**
	 * Registers an update callback for runtime render-frame ticks.
	 *
	 * The callback is invoked once per render-frame tick while no previous update
	 * is still running. If an earlier update has not completed, the current tick is
	 * skipped.
	 *
	 * The `timestep` parameter is the elapsed time, in milliseconds, since the
	 * previous update callback started.
	 *
	 * @param onUpdate - The callback invoked on each scheduled render-frame tick.
	 */
	ScheduleUpdate(onUpdate: (timestep: number, project: Project) => void | Promise<void>): void;
	/**
	 * Launches the project.
	 *
	 * Adamas API calls are not available until internal initialization has
	 * completed. The `onSetup` callback is invoked after initialization, and
	 * application code may safely call Adamas APIs from within `onSetup` and any
	 * functions executed after it.
	 *
	 * @param onSetup - Called after project initialization to configure the scene
	 * and register project logic.
	 * @returns A promise that resolves when the project launch has completed.
	 */
	Launch(onSetup: (sceneGraph: SceneGraph, project: Project) => Promise<void>): Promise<void>;
}
/**
 * Opaque numeric handle that identifies a device subscription.
 */
export type DeviceSubscription = number;
/**
 * Built-in device input paths.
 */
export declare const DevicePath: {
	/** Left grip value in the range `[0.0, 1.0]`. */
	LEFT_GRIP: string;
	/** Left trigger value in the range `[0.0, 1.0]`. */
	LEFT_TRIGGER: string;
	/** Left primary 2D axis as a `vec2`, with each component in the range `[-1, 1]`. */
	LEFT_PRIMARY_2D_AXIS: string;
	/** Left primary 2D axis button value, either `0` or `1`. */
	LEFT_PRIMARY_2D_AXIS_BUTTON: string;
	/** Left primary button value, either `0` or `1`. */
	LEFT_PRIMARY_BUTTON: string;
	/** Left secondary button value, either `0` or `1`. */
	LEFT_SECONDARY_BUTTON: string;
	/** Right grip value in the range `[0.0, 1.0]`. */
	RIGHT_GRIP: string;
	/** Right trigger value in the range `[0.0, 1.0]`. */
	RIGHT_TRIGGER: string;
	/** Right primary 2D axis as a `vec2`, with each component in the range `[-1, 1]`. */
	RIGHT_PRIMARY_2D_AXIS: string;
	/** Right primary 2D axis button value, either `0` or `1`. */
	RIGHT_PRIMARY_2D_AXIS_BUTTON: string;
	/** Right primary button value, either `0` or `1`. */
	RIGHT_PRIMARY_BUTTON: string;
	/** Right secondary button value, either `0` or `1`. */
	RIGHT_SECONDARY_BUTTON: string;
};
/**
 * Reads and subscribes to device input values.
 */
export declare class Device {
	/**
	 * Gets the current value of a device path.
	 *
	 * @param devicePath - The device path to read.
	 * @returns A promise that resolves to the current input value, or `undefined`
	 * if no value is available.
	 */
	static GetValue(devicePath: string): Promise<number | vec2 | undefined>;
	/**
	 * Subscribes to value changes for a device path.
	 *
	 * @param devicePath - The device path to observe.
	 * @param callback - The callback that receives updated input values.
	 * @returns A promise that resolves to the {@link DeviceSubscription} handle.
	 */
	static SubscribeValueChange(devicePath: string, callback: (value: number | vec2) => void): Promise<number>;
	/**
	 * Unsubscribes from a device value change subscription.
	 *
	 * @param handle - The {@link DeviceSubscription} handle to remove.
	 * @returns A promise that resolves to `true` if the subscription was removed,
	 * or `false` otherwise.
	 */
	static UnsubscribeValueChange(handle: DeviceSubscription): Promise<boolean>;
}
/**
 * Represents a user.
 */
export declare class User {
	private userId;
	/**
	 * Creates a user instance.
	 *
	 * @param userId - The user ID.
	 */
	constructor(userId: string);
	/**
	 * Gets the local user.
	 *
	 * @returns A promise that resolves to the local {@link User}.
	 */
	static GetLocalUser(): Promise<User>;
	/**
	 * Gets all users.
	 *
	 * @returns A promise that resolves to the list of {@link User} instances.
	 */
	static GetUsers(): Promise<User[]>;
	/**
	 * Gets the user ID.
	 *
	 * @returns The user ID.
	 */
	GetUserId(): string;
	/**
	 * Gets the head entity of the user.
	 *
	 * @returns A promise that resolves to the head {@link Entity}.
	 */
	GetHeadEntity(): Promise<number>;
	/**
	 * Gets the left hand entity of the user.
	 *
	 * @returns A promise that resolves to the left hand {@link Entity}.
	 */
	GetLeftHandEntity(): Promise<number>;
	/**
	 * Gets the right hand entity of the user.
	 *
	 * @returns A promise that resolves to the right hand {@link Entity}.
	 */
	GetRightHandEntity(): Promise<number>;
	/**
	 * Gets the origin entity of the user.
	 *
	 * @returns A promise that resolves to the origin {@link Entity}.
	 */
	GetOriginEntity(): Promise<number>;
	/**
	 * Sets whether locomotion is enabled for the user.
	 *
	 * @param value - Whether locomotion is enabled.
	 * @returns A promise that resolves when the locomotion setting has been
	 * changed.
	 */
	SetEnableLocomotion(value: boolean): Promise<void>;
	/**
	 * Gets whether locomotion is enabled for the user.
	 *
	 * @returns A promise that resolves to `true` if locomotion is enabled, or
	 * `false` otherwise.
	 */
	GetEnableLocomotion(): Promise<boolean>;
	/**
	 * Teleports the user to a world position and rotation.
	 *
	 * @param worldPosition - The destination world position.
	 * @param worldRotation - The destination world rotation.
	 * @returns A promise that resolves when the user has been teleported.
	 */
	TeleportTo(worldPosition: vec3, worldRotation: quat): Promise<void>;
	/**
	 * Gets the world position of the user.
	 *
	 * @returns A promise that resolves to the user world position.
	 */
	GetUserWorldPosition(): Promise<vec3>;
	/**
	 * Gets the world rotation of the user as a quaternion.
	 *
	 * @returns A promise that resolves to the user world rotation.
	 */
	GetUserWorldRotation(): Promise<quat>;
	/**
	 * Checks whether the user is valid.
	 *
	 * A referenced user may become invalid after leaving a network session.
	 *
	 * @returns A promise that resolves to `true` if the user is valid, or `false`
	 * otherwise.
	 */
	IsValid(): Promise<boolean>;
	/**
	 * Checks whether the user is the local user.
	 *
	 * Remote users in a network session are not local users.
	 *
	 * @returns A promise that resolves to `true` if the user is the local user, or
	 * `false` otherwise.
	 */
	IsLocal(): Promise<boolean>;
	/**
	 * Checks whether the user is in VR mode.
	 *
	 * Users in PC mode return `false`.
	 *
	 * @returns A promise that resolves to `true` if the user is in VR mode, or
	 * `false` otherwise.
	 */
	IsVRMode(): Promise<boolean>;
	/**
	 * Checks whether the user menu is open.
	 *
	 * @returns A promise that resolves to `true` if the user menu is open, or
	 * `false` otherwise.
	 */
	IsMenuOpen(): Promise<boolean>;
	/**
	 * Checks whether the user is grounded.
	 *
	 * @returns A promise that resolves to `true` if the user is grounded, or
	 * `false` otherwise.
	 */
	IsGrounded(): Promise<boolean>;
	/**
	 * Checks whether the user is flying.
	 *
	 * @returns A promise that resolves to `true` if the user is flying, or `false`
	 * otherwise.
	 */
	IsFlying(): Promise<boolean>;
	/**
	 * Gets the move speed of the user.
	 *
	 * @returns A promise that resolves to the move speed in meters per second.
	 */
	GetMoveSpeed(): Promise<number>;
	/**
	 * Sets the move speed of the user.
	 *
	 * @param speed - The move speed in meters per second.
	 * @returns A promise that resolves when the move speed has been changed.
	 */
	SetMoveSpeed(speed: number): Promise<void>;
	/**
	 * Gets the jump velocity of the user.
	 *
	 * @returns A promise that resolves to the jump velocity in meters per second.
	 */
	GetJumpVelocity(): Promise<number>;
	/**
	 * Sets the jump velocity of the user.
	 *
	 * @param velocity - The jump velocity in meters per second.
	 * @returns A promise that resolves when the jump velocity has been changed.
	 */
	SetJumpVelocity(velocity: number): Promise<void>;
	/**
	 * Gets the gravity strength of the user.
	 *
	 * The final gravity applied to the user is calculated by multiplying this
	 * value by the default Earth gravity.
	 *
	 * @returns A promise that resolves to the gravity strength.
	 */
	GetGravityStrength(): Promise<number>;
	/**
	 * Sets the gravity strength of the user.
	 *
	 * The final gravity applied to the user is calculated by multiplying this
	 * value by the default Earth gravity.
	 *
	 * @param strength - The gravity strength.
	 * @returns A promise that resolves when the gravity strength has been changed.
	 */
	SetGravityStrength(strength: number): Promise<void>;
}
/**
 * Mesh asset data for a cube.
 */
export declare const cubeMeshAsset: {
	base64Indices: string;
	base64Vertices: string;
	base64Normals: string;
	base64Uvs: string;
};
/**
 * Mesh asset data for a quad.
 */
export declare const quadMeshAsset: {
	base64Indices: string;
	base64Vertices: string;
	base64Normals: string;
	base64Uvs: string;
};
/**
 * Mesh asset data for a sphere.
 */
export declare const sphereMeshAsset: {
	base64Indices: string;
	base64Vertices: string;
	base64Normals: string;
	base64Uvs: string;
};
/**
 * Mesh asset data for a capsule.
 */
export declare const capsuleMeshAsset: {
	base64Indices: string;
	base64Vertices: string;
	base64Normals: string;
	base64Uvs: string;
};
/**
 * Mesh asset data for a cylinder.
 */
export declare const cylinderMeshAsset: {
	base64Indices: string;
	base64Vertices: string;
	base64Normals: string;
	base64Uvs: string;
};
/**
 * Creates a cube mesh.
 *
 * @returns A promise that resolves to the created mesh handle.
 */
export declare function NewCubeMesh(): Promise<number>;
/**
 * Creates a quad mesh.
 *
 * @returns A promise that resolves to the created mesh handle.
 */
export declare function NewQuadMesh(): Promise<number>;
/**
 * Creates a sphere mesh.
 *
 * @returns A promise that resolves to the created mesh handle.
 */
export declare function NewSphereMesh(): Promise<number>;
/**
 * Creates a capsule mesh.
 *
 * @returns A promise that resolves to the created mesh handle.
 */
export declare function NewCapsuleMesh(): Promise<number>;
/**
 * Creates a cylinder mesh.
 *
 * @returns A promise that resolves to the created mesh handle.
 */
export declare function NewCylinderMesh(): Promise<number>;
/**
 * Reads and updates entity transforms.
 */
export declare class TransformManager {
	/**
	 * Rotates an entity so it faces a world position.
	 *
	 * @param entity - The {@link Entity} to rotate.
	 * @param worldPosition - The world position to face.
	 * @param worldUp - The upward direction to use. Defaults to `[0, 1, 0]`.
	 * @returns A promise that resolves when the rotation has been changed.
	 */
	static LookAt(entity: Entity, worldPosition: vec3, worldUp?: vec3): Promise<void>;
	/**
	 * Sets the parent of an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param parent - The parent {@link Entity}. If omitted, the entity will have
	 * no parent.
	 * @returns A promise that resolves when the parent has been changed.
	 */
	static SetParent(entity: Entity, parent?: Entity): Promise<void>;
	/**
	 * Gets the parent of an entity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the parent {@link Entity}, or
	 * `undefined` if the entity has no parent.
	 */
	static GetParent(entity: Entity): Promise<number | undefined>;
	/**
	 * Sets the world position of an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param pos - The world position.
	 * @returns A promise that resolves when the world position has been changed.
	 */
	static SetWorldPosition(entity: Entity, pos: vec3): Promise<void>;
	/**
	 * Sets the local position of an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param pos - The local position.
	 * @returns A promise that resolves when the local position has been changed.
	 */
	static SetLocalPosition(entity: Entity, pos: vec3): Promise<void>;
	/**
	 * Gets the world position of an entity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the world position.
	 */
	static GetWorldPosition(entity: Entity): Promise<vec3>;
	/**
	 * Gets the local position of an entity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the local position.
	 */
	static GetLocalPosition(entity: Entity): Promise<vec3>;
	/**
	 * Sets the world rotation of an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param rotation - The world rotation quaternion in `[x, y, z, w]` order.
	 * @returns A promise that resolves when the world rotation has been changed.
	 */
	static SetWorldRotation(entity: Entity, rotation: quat): Promise<void>;
	/**
	 * Sets the local rotation of an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param rotation - The local rotation quaternion in `[x, y, z, w]` order.
	 * @returns A promise that resolves when the local rotation has been changed.
	 */
	static SetLocalRotation(entity: Entity, rotation: quat): Promise<void>;
	/**
	 * Gets the world rotation of an entity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the world rotation quaternion.
	 */
	static GetWorldRotation(entity: Entity): Promise<quat>;
	/**
	 * Gets the local rotation of an entity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the local rotation quaternion.
	 */
	static GetLocalRotation(entity: Entity): Promise<quat>;
	/**
	 * Sets the local scale of an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param scale - The local scale.
	 * @returns A promise that resolves when the local scale has been changed.
	 */
	static SetLocalScale(entity: Entity, scale: vec3): Promise<void>;
	/**
	 * Gets the local scale of an entity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the local scale.
	 */
	static GetLocalScale(entity: Entity): Promise<vec3>;
}
/**
 * Updates renderer settings.
 */
export declare class RendererManager {
	/**
	 * Sets the skybox texture.
	 *
	 * @param texture - The skybox {@link Texture}; it must be a 2D texture.
	 * @returns A promise that resolves when the skybox texture has been changed.
	 */
	static SetSkybox2DTexture(texture: Texture): Promise<void>;
	/**
	 * Gets the skybox texture.
	 *
	 * @returns A promise that resolves to the skybox {@link Texture}.
	 */
	static GetSkybox2DTexture(): Promise<number>;
	/**
	 * Renders a cubemap from a position.
	 *
	 * @param texture - The {@link Texture} to render into. It must be a render texture with a cube dimesion.
	 * @param position - The world position to render from. Defaults to `[0, 0, 0]`.
	 * @returns A promise that resolves when the cubemap has been rendered.
	 */
	static RenderCubemap(texture: Texture, position?: vec3): Promise<void>;
	/**
	 * Sets the reflection cubemap.
	 *
	 * @param texture - The {@link Texture} to assign as the reflection cubemap. It must have a cube dimesion.
	 * @returns A promise that resolves when the reflection cubemap has been
	 * changed.
	 */
	static SetReflectionCubemap(texture: Texture): Promise<void>;
	/**
	 * Gets the reflection cubemap.
	 *
	 * @returns A promise that resolves to the reflection cubemap {@link Texture}.
	 */
	static GetReflectionCubemap(): Promise<number>;
	/**
	 * Sets the environment intensity.
	 *
	 * @param intensity - The environment intensity as a number in the range
	 * `[0, 10]`, where `0` means no environment lighting and reflection.
	 * @returns A promise that resolves when the environment intensity has been
	 * changed.
	 */
	static SetEnvironmentIntensity(intensity: number): Promise<void>;
	/**
	 * Gets the environment intensity.
	 *
	 * @returns A promise that resolves to the environment intensity.
	 */
	static GetEnvironmentIntensity(): Promise<number>;
}
/**
 * Shared 2D GPU texture that is accessible from the local project and the
 * Adamas runtime.
 *
 * The underlying shared-memory pixel format is always 8-bit-per-channel
 * `RGBA`.
 *
 * This texture can be used in the same manner as a 2D texture created by
 * `TextureManager.Create2D`, including APIs that accept a texture handle for
 * rendering or material assignment.
 *
 * Color space is controlled by the `linear` argument:
 * - `false` (default): sRGB texture
 * - `true`: linear texture
 *
 * Use {@link uploadRGBA} and {@link readLatestRGBA} with `Uint8Array` data in
 * `R`, `G`, `B`, `A` byte order.
 */
export declare class SharedTexture {
	private readonly addon;
	private readonly nativeHandle;
	private readonly createResult;
	private readonly parsedShareToken;
	private closed;
	private constructor();
	/**
	 * Creates a shared GPU texture.
	 *
	 * The underlying native shared-memory layout is `RGBA8`.
	 *
	 * @param width - Texture width in pixels.
	 * @param height - Texture height in pixels.
	 * @param linear - Whether the shared texture should use linear color space.
	 * Defaults to `false`, which creates an sRGB texture.
	 * @returns A promise that resolves to the created shared texture wrapper.
	 */
	static Create(width: number, height: number, linear?: boolean): Promise<SharedTexture>;
	/**
	 * Gets the texture handle for this shared texture.
	 *
	 * This handle can be passed to other render APIs that accept a texture handle.
	 */
	get textureHandle(): Texture;
	/**
	 * Gets the width of the shared texture in pixels.
	 */
	get width(): number;
	/**
	 * Gets the height of the shared texture in pixels.
	 */
	get height(): number;
	/**
	 * Gets whether the shared texture uses linear color space.
	 *
	 * When `false`, the shared texture uses sRGB color space.
	 */
	get linear(): boolean;
	/**
	 * Gets the native texture id for this shared texture.
	 *
	 * This value can be forwarded to other native addons that coordinate with
	 * the same shared GPU resource.
	 *
	 * The underlying native backend is:
	 * - macOS: `metal-iosurface`
	 * - Windows: `d3d11`
	 */
	get nativeTextureId(): number;
	/**
	 * Uploads a full frame into the shared texture.
	 *
	 * The input data must be tightly packed 8-bit `RGBA` bytes in `R`, `G`, `B`,
	 * `A` order. The byte length must be exactly `width * height * 4`.
	 *
	 * @param rgbaData - Raw `RGBA8` pixel data for the full texture.
	 * @returns A promise that resolves when the shared texture has been updated.
	 */
	uploadRGBA(rgbaData: Uint8Array): Promise<void>;
	/**
	 * Copies a render texture into this shared texture on the runtime side.
	 *
	 * @param renderTextureHandle - Render texture handle to copy from.
	 * @returns A promise that resolves when the GPU copy request has completed.
	 */
	blitFromRenderTexture(renderTexture: Texture): Promise<void>;
	/**
	 * Reads the latest full frame from the shared texture.
	 *
	 * The returned data is tightly packed 8-bit `RGBA` bytes in `R`, `G`, `B`,
	 * `A` order, matching the shared texture's true native memory format.
	 *
	 * @returns The latest full-frame `RGBA8` pixel buffer.
	 */
	readRGBA(): Uint8Array;
	/**
	 * Destroys the shared texture and releases its native resources.
	 *
	 * After this completes, the instance must not be used again.
	 *
	 * @returns A promise that resolves after both the runtime-side and local
	 * native shared-texture resources have been released.
	 */
	close(): Promise<void>;
	private throwIfClosed;
}
/**
 * Supported force application modes.
 */
export declare enum ForceMode {
	/** Add a continuous force to the rigidbody, using its mass. */
	Force = 0,
	/** Add a continuous acceleration to the rigidbody, ignoring its mass. */
	Acceleration = 5,
	/** Add an instant force impulse to the rigidbody, using its mass. */
	Impulse = 1,
	/** Add an instant velocity change to the rigidbody, ignoring its mass. */
	VelocityChange = 2
}
/**
 * Creates and updates rigidbody components.
 */
export declare class RigidbodyManager {
	/**
	 * Creates a rigidbody component on an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the rigidbody component was
	 * created, or `false` otherwise.
	 */
	static Create(entity: Entity): Promise<boolean>;
	/**
	 * Removes a rigidbody component from an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the rigidbody component was
	 * removed, or `false` otherwise.
	 */
	static Destroy(entity: Entity): Promise<boolean>;
	/**
	 * Checks whether an entity has a rigidbody component.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the entity has a rigidbody
	 * component, or `false` otherwise.
	 */
	static HasComponent(entity: Entity): Promise<boolean>;
	/**
	 * Gets the linear velocity of a rigidbody.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the linear velocity in meters per
	 * second.
	 */
	static GetLinearVelocity(entity: Entity): Promise<vec3>;
	/**
	 * Gets the angular velocity of a rigidbody.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the angular velocity in radians per
	 * second.
	 */
	static GetAngularVelocity(entity: Entity): Promise<vec3>;
	/**
	 * Adds force to a rigidbody.
	 *
	 * The force is expressed in newtons for {@link ForceMode.Force}, meters per
	 * second squared for {@link ForceMode.Acceleration}, newton-seconds for
	 * {@link ForceMode.Impulse}, and meters per second for
	 * {@link ForceMode.VelocityChange}.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param force - The force vector.
	 * @param forceMode - The force application mode. Defaults to
	 * {@link ForceMode.Force}.
	 * @returns A promise that resolves when the force has been applied.
	 */
	static AddForce(entity: Entity, force: vec3, forceMode?: ForceMode): Promise<void>;
	/**
	 * Adds force to a rigidbody at a world position.
	 *
	 * The force is expressed in newtons for {@link ForceMode.Force}, meters per
	 * second squared for {@link ForceMode.Acceleration}, newton-seconds for
	 * {@link ForceMode.Impulse}, and meters per second for
	 * {@link ForceMode.VelocityChange}.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param force - The force vector.
	 * @param position - The world position where the force is applied, in meters.
	 * @param forceMode - The force application mode. Defaults to
	 * {@link ForceMode.Force}.
	 * @returns A promise that resolves to `true` if the force was applied, or
	 * `false` otherwise.
	 */
	static AddForceAtPosition(entity: Entity, force: vec3, position: vec3, forceMode?: ForceMode): Promise<boolean>;
	/**
	 * Adds torque to a rigidbody.
	 *
	 * The torque is expressed in newton-meters for {@link ForceMode.Force},
	 * radians per second squared for {@link ForceMode.Acceleration},
	 * newton-meter-seconds for {@link ForceMode.Impulse}, and radians per second
	 * for {@link ForceMode.VelocityChange}.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param torque - The torque vector.
	 * @param forceMode - The force application mode. Defaults to
	 * {@link ForceMode.Force}.
	 * @returns A promise that resolves when the torque has been applied.
	 */
	static AddTorque(entity: Entity, torque: vec3, forceMode?: ForceMode): Promise<void>;
	/**
	 * Gets whether a rigidbody is kinematic.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the rigidbody is kinematic, or
	 * `false` otherwise.
	 */
	static GetIsKinematic(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a rigidbody is kinematic.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param isKinematic - Whether the rigidbody is kinematic.
	 * @returns A promise that resolves when the kinematic setting has been
	 * changed.
	 */
	static SetIsKinematic(entity: Entity, isKinematic: boolean): Promise<void>;
	/**
	 * Gets whether a rigidbody uses gravity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the rigidbody uses gravity, or
	 * `false` otherwise.
	 */
	static GetUseGravity(entity: Entity): Promise<boolean>;
	/**
	 * Sets whether a rigidbody uses gravity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param useGravity - Whether the rigidbody uses gravity.
	 * @returns A promise that resolves when the gravity setting has been changed.
	 */
	static SetUseGravity(entity: Entity, useGravity: boolean): Promise<void>;
	/**
	 * Gets the mass of a rigidbody.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the mass in kilograms.
	 */
	static GetMass(entity: Entity): Promise<number>;
	/**
	 * Sets the mass of a rigidbody.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param mass - The mass in kilograms.
	 * @returns A promise that resolves when the mass has been changed.
	 */
	static SetMass(entity: Entity, mass: number): Promise<void>;
	/**
	 * Gets the linear damping of a rigidbody.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the linear damping.
	 */
	static GetLinearDamping(entity: Entity): Promise<number>;
	/**
	 * Sets the linear damping of a rigidbody.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param linearDamping - The linear damping.
	 * @returns A promise that resolves when the linear damping has been changed.
	 */
	static SetLinearDamping(entity: Entity, linearDamping: number): Promise<void>;
	/**
	 * Gets the angular damping of a rigidbody.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the angular damping.
	 */
	static GetAngularDamping(entity: Entity): Promise<number>;
	/**
	 * Sets the angular damping of a rigidbody.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param angularDamping - The angular damping.
	 * @returns A promise that resolves when the angular damping has been changed.
	 */
	static SetAngularDamping(entity: Entity, angularDamping: number): Promise<void>;
}
/**
 * Opaque numeric handle that identifies a collider.
 */
export type Collider = number;
/**
 * Creates and updates colliders and collider event callbacks.
 */
export declare class ColliderManager {
	/**
	 * Creates a box collider on an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to the created {@link Collider}.
	 */
	static CreateBox(entity: Entity): Promise<number>;
	/**
	 * Creates a sphere collider on an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to the created {@link Collider}.
	 */
	static CreateSphere(entity: Entity): Promise<number>;
	/**
	 * Creates a capsule collider on an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @returns A promise that resolves to the created {@link Collider}.
	 */
	static CreateCapsule(entity: Entity): Promise<number>;
	/**
	 * Destroys a collider on an entity.
	 *
	 * @param entity - The {@link Entity} to update.
	 * @param collider - The {@link Collider} to destroy.
	 * @returns A promise that resolves to `true` if the collider was destroyed, or
	 * `false` otherwise.
	 */
	static Destroy(entity: Entity, collider: Collider): Promise<boolean>;
	/**
	 * Checks whether an entity has a collider.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @param collider - The {@link Collider} to check.
	 * @returns A promise that resolves to `true` if the entity has the collider, or
	 * `false` otherwise.
	 */
	static HasComponent(entity: Entity, collider: Collider): Promise<boolean>;
	/**
	 * Gets the entity that owns a collider.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to the owning {@link Entity}.
	 */
	static GetEntity(collider: Collider): Promise<number>;
	/**
	 * Gets all colliders on an entity.
	 *
	 * @param entity - The {@link Entity} to inspect.
	 * @returns A promise that resolves to the list of {@link Collider} handles.
	 */
	static GetAllColliders(entity: Entity): Promise<number[]>;
	/**
	 * Gets the center of a box collider.
	 *
	 * An error is thrown if the collider is not a box collider.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to the box collider center.
	 */
	static GetBoxColliderCenter(collider: Collider): Promise<vec3>;
	/**
	 * Sets the center of a box collider.
	 *
	 * An error is thrown if the collider is not a box collider.
	 *
	 * @param collider - The {@link Collider} to update.
	 * @param center - The box collider center.
	 * @returns A promise that resolves when the center has been changed.
	 */
	static SetBoxColliderCenter(collider: Collider, center: vec3): Promise<void>;
	/**
	 * Gets the size of a box collider.
	 *
	 * An error is thrown if the collider is not a box collider.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to the box collider size.
	 */
	static GetBoxColliderSize(collider: Collider): Promise<vec3>;
	/**
	 * Sets the size of a box collider.
	 *
	 * An error is thrown if the collider is not a box collider.
	 *
	 * @param collider - The {@link Collider} to update.
	 * @param size - The box collider size.
	 * @returns A promise that resolves when the size has been changed.
	 */
	static SetBoxColliderSize(collider: Collider, size: vec3): Promise<void>;
	/**
	 * Gets the center of a sphere collider.
	 *
	 * An error is thrown if the collider is not a sphere collider.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to the sphere collider center.
	 */
	static GetSphereColliderCenter(collider: Collider): Promise<vec3>;
	/**
	 * Sets the center of a sphere collider.
	 *
	 * An error is thrown if the collider is not a sphere collider.
	 *
	 * @param collider - The {@link Collider} to update.
	 * @param center - The sphere collider center.
	 * @returns A promise that resolves when the center has been changed.
	 */
	static SetSphereColliderCenter(collider: Collider, center: vec3): Promise<void>;
	/**
	 * Gets the radius of a sphere collider.
	 *
	 * An error is thrown if the collider is not a sphere collider.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to the sphere collider radius.
	 */
	static GetSphereColliderRadius(collider: Collider): Promise<number>;
	/**
	 * Sets the radius of a sphere collider.
	 *
	 * An error is thrown if the collider is not a sphere collider.
	 *
	 * @param collider - The {@link Collider} to update.
	 * @param radius - The sphere collider radius.
	 * @returns A promise that resolves when the radius has been changed.
	 */
	static SetSphereColliderRadius(collider: Collider, radius: number): Promise<void>;
	/**
	 * Gets the center of a capsule collider.
	 *
	 * An error is thrown if the collider is not a capsule collider.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to the capsule collider center.
	 */
	static GetCapsuleColliderCenter(collider: Collider): Promise<vec3>;
	/**
	 * Sets the center of a capsule collider.
	 *
	 * An error is thrown if the collider is not a capsule collider.
	 *
	 * @param collider - The {@link Collider} to update.
	 * @param center - The capsule collider center.
	 * @returns A promise that resolves when the center has been changed.
	 */
	static SetCapsuleColliderCenter(collider: Collider, center: vec3): Promise<void>;
	/**
	 * Gets the radius of a capsule collider.
	 *
	 * An error is thrown if the collider is not a capsule collider.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to the capsule collider radius.
	 */
	static GetCapsuleColliderRadius(collider: Collider): Promise<number>;
	/**
	 * Sets the radius of a capsule collider.
	 *
	 * An error is thrown if the collider is not a capsule collider.
	 *
	 * @param collider - The {@link Collider} to update.
	 * @param radius - The capsule collider radius.
	 * @returns A promise that resolves when the radius has been changed.
	 */
	static SetCapsuleColliderRadius(collider: Collider, radius: number): Promise<void>;
	/**
	 * Gets the height of a capsule collider.
	 *
	 * An error is thrown if the collider is not a capsule collider.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to the capsule collider height.
	 */
	static GetCapsuleColliderHeight(collider: Collider): Promise<number>;
	/**
	 * Sets the height of a capsule collider.
	 *
	 * An error is thrown if the collider is not a capsule collider.
	 *
	 * @param collider - The {@link Collider} to update.
	 * @param height - The capsule collider height.
	 * @returns A promise that resolves when the height has been changed.
	 */
	static SetCapsuleColliderHeight(collider: Collider, height: number): Promise<void>;
	/**
	 * Gets whether a collider is a trigger.
	 *
	 * @param collider - The {@link Collider} to inspect.
	 * @returns A promise that resolves to `true` if the collider is a trigger, or
	 * `false` otherwise.
	 */
	static GetIsTrigger(collider: Collider): Promise<boolean>;
	/**
	 * Sets whether a collider is a trigger.
	 *
	 * @param collider - The {@link Collider} to update.
	 * @param isTrigger - Whether the collider is a trigger.
	 * @returns A promise that resolves when the trigger setting has been changed.
	 */
	static SetIsTrigger(collider: Collider, isTrigger: boolean): Promise<void>;
	/**
	 * Registers a callback for trigger enter events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onTriggerEnter - The callback invoked when another collider enters
	 * the trigger.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnTriggerEnter(entity: Entity, onTriggerEnter: (entity: Entity, collider: Collider) => void): Promise<boolean>;
	/**
	 * Registers a callback for trigger exit events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onTriggerExit - The callback invoked when another collider exits the
	 * trigger.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnTriggerExit(entity: Entity, onTriggerExit: (entity: Entity, collider: Collider) => void): Promise<boolean>;
	/**
	 * Registers a callback for trigger stay events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onTriggerStay - The callback invoked while another collider remains
	 * in the trigger.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnTriggerStay(entity: Entity, onTriggerStay: (entity: Entity, collider: Collider) => void): Promise<boolean>;
	/**
	 * Registers a callback for collision enter events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onCollisionEnter - The callback invoked when another collider begins
	 * colliding.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnCollisionEnter(entity: Entity, onCollisionEnter: (entity: Entity, collider: Collider) => void): Promise<boolean>;
	/**
	 * Registers a callback for collision exit events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onCollisionExit - The callback invoked when another collider stops
	 * colliding.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnCollisionExit(entity: Entity, onCollisionExit: (entity: Entity, collider: Collider) => void): Promise<boolean>;
	/**
	 * Registers a callback for collision stay events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onCollisionStay - The callback invoked while another collider remains
	 * in contact.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnCollisionStay(entity: Entity, onCollisionStay: (entity: Entity, collider: Collider) => void): Promise<boolean>;
	/**
	 * Registers a callback for user trigger enter events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onUserTriggerEnter - The callback invoked when a user enters the
	 * trigger.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnUserTriggerEnter(entity: Entity, onUserTriggerEnter: (user: User) => void): Promise<boolean>;
	/**
	 * Registers a callback for user trigger exit events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onUserTriggerExit - The callback invoked when a user exits the
	 * trigger.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnUserTriggerExit(entity: Entity, onUserTriggerExit: (user: User) => void): Promise<boolean>;
	/**
	 * Registers a callback for user trigger stay events.
	 *
	 * @param entity - The {@link Entity} to observe.
	 * @param onUserTriggerStay - The callback invoked while a user remains in the
	 * trigger.
	 * @returns A promise that resolves to `true` if the callback was registered,
	 * or `false` otherwise.
	 */
	static OnUserTriggerStay(entity: Entity, onUserTriggerStay: (user: User) => void): Promise<boolean>;
}
export type NonUndefined<T> = T extends undefined ? never : T;
/**
 * Network state.
 */
export interface NetworkState<T> {
	/** Current state value. */
	value: NonUndefined<T>;
}
/**
 * Provides networking utilities for state synchronization, messaging, and user
 * session events.
 *
 * For real-time network data updates, including repeated networking calls or
 * network variable updates in an loop, an update frequency of
 * approximately 10 to 20 Hz is recommended.
 */
export declare class Networking {
	#private;
	/**
	 * Gets the network identifier for the current project session.
	 *
	 * @returns The network identifier.
	 */
	static GetNetworkID(): string;
	/**
	 * Checks whether networking is running in local mode.
	 *
	 * @returns A promise that resolves to `true` if local mode is enabled, or
	 * `false` otherwise.
	 */
	static IsLocalMode(): Promise<boolean>;
	/**
	 * Checks whether the current client is the master client.
	 *
	 * The master client is responsible for session-level authority and may change
	 * during a network session. If the current master client leaves, a new master
	 * client is automatically selected. In a local session, this method always
	 * returns `true`.
	 *
	 * @returns A promise that resolves to `true` if the current client is the
	 * master client, or `false` otherwise.
	 */
	static IsMasterClient(): Promise<boolean>;
	/**
	 * Gets the current client ID. Each client in a network session is
	 * assigned a unique ID that can be used for identification and
	 * session-level networking operations.
	 *
	 * In a local session, this method alreays returns `1`.
	 *
	 * @returns A promise that resolves to the current client ID.
	 */
	static GetClientId(): Promise<number>;
	/**
	 * Gets the master client ID.
	 *
	 * In a local session, this method always returns `1`.
	 *
	 * @returns A promise that resolves to the master client ID.
	 */
	static GetMasterClientId(): Promise<number>;
	/**
	 * Makes an entity use a network transform.
	 *
	 * The transform is synchronized across clients that have joined the same
	 * network session. In a local session, this method has no effect and does not
	 * throw an error.
	 *
	 * @param entityHandle - The {@link Entity} to update.
	 * @returns A promise that resolves to `true` if the network transform was
	 * created, or `false` otherwise.
	 */
	static MakeNetworkTransform(entityHandle: Entity): Promise<boolean>;
	/**
	 * Checks whether an entity uses a network transform.
	 *
	 * @param entityHandle - The {@link Entity} to inspect.
	 * @returns A promise that resolves to `true` if the entity uses a network
	 * transform, or `false` otherwise.
	 */
	static IsNetworkTransform(entityHandle: Entity): Promise<boolean>;
	/**
	 * Synchronizes the local transform of an entity to the network session.
	 *
	 * The client that calls this method updates the local entity transform for all
	 * clients in the same network session. In a local session, this method has no
	 * effect and does not throw an error.
	 *
	 * @param entityHandle - The {@link Entity} to synchronize.
	 * @returns A promise that resolves to `true` if the transform was
	 * synchronized, or `false` otherwise.
	 */
	static SyncLocalTransform(entityHandle: Entity): Promise<boolean>;
	/**
	 * Creates a named message channel.
	 *
	 * In a local session, the channel can still be created, but it does not receive
	 * messages, including messages sent by the local client.
	 *
	 * @param channelName - The channel name.
	 * @param onReceived - The callback invoked when a message is received.
	 * @returns A promise that resolves when the channel has been created.
	 */
	static NewChannel(channelName: string, onReceived: (sender: number, payload: string) => void): Promise<void>;
	/**
	 * Sends a message to a specific player on a named channel.
	 *
	 * In a local session, this method has no effect and does not throw an error.
	 *
	 * @param playerId - The target player ID.
	 * @param channelName - The channel name.
	 * @param payload - The message payload.
	 * @returns A promise that resolves when the message has been sent.
	 */
	static SendMessageTo(playerId: number, channelName: string, payload: string): Promise<void>;
	/**
	 * Broadcasts a message to all players on a named channel.
	 *
	 * In a local session, this method has no effect and does not throw an error.
	 *
	 * @param channelName - The channel name.
	 * @param payload - The message payload.
	 * @returns A promise that resolves when the message has been broadcast.
	 */
	static BroadcastMessage(channelName: string, payload: string): Promise<void>;
	/**
	 * Creates a synchronized network variable.
	 *
	 * The returned state object holds the same value for all clients in the same
	 * network session. Changes to `value` are broadcast to the session. In a local
	 * session, the variable is still created, and the local `onStateChange`
	 * callback is invoked when the state value changes.
	 *
	 * `undefined` is not a valid state value. Use `null` instead when the state needs
	 * to represent an intentionally empty value.
	 *
	 * @param initialValue - The initial state value. Must not be `undefined`.
	 * @param onStateChange - The callback invoked when the state value changes.
	 * It is also invoked when the initial value is first assigned to the network
	 * state.
	 * @returns The synchronized {@link NetworkState}.
	 */
	static NewVariable<T>(initialValue: NonUndefined<T>, onStateChange?: (state: NonUndefined<T>) => void): NetworkState<T>;
	/**
	 * Creates a synchronized network function.
	 *
	 * Calling the returned function locally broadcasts the call arguments to the
	 * network session. In a local session, the function is still
	 * created and remains callable by the local client.
	 *
	 * @param func - The function to synchronize.
	 * @returns The synchronized function.
	 */
	static NewFunction<F extends (...args: any[]) => any>(func: F): (...args: Parameters<F>) => void;
	/**
	 * Registers a callback for user join events.
	 *
	 * @param onUserJoined - The callback invoked when a user joins.
	 * @returns A promise that resolves when the callback has been registered.
	 */
	static OnUserJoined(onUserJoined: (user: User) => void): Promise<void>;
	/**
	 * Registers a callback for user leave events.
	 *
	 * @param onUserLeft - The callback invoked when a user leaves.
	 * @returns A promise that resolves when the callback has been registered.
	 */
	static OnUserLeft(onUserLeft: (user: User) => void): Promise<void>;
}
/**
 * Encodes binary data as a base64 string.
 *
 * @param input - The binary data to encode.
 * @returns The base64-encoded string.
 */
export declare const base64Encode: (input: ArrayBuffer | ArrayBufferView) => string;
/**
 * Encodes binary data as a base64 string.
 *
 * @param input - The binary data to encode.
 * @returns A promise that resolves to the base64-encoded string.
 */
export declare const base64EncodeAsync: (input: ArrayBuffer | ArrayBufferView) => Promise<string>;
/**
 * Decodes a base64 string into binary data.
 *
 * @param base64 - The base64 string to decode.
 * @returns The decoded binary data.
 */
export declare const base64Decode: (base64: string) => ArrayBuffer;
/**
 * Decodes a base64 string into binary data.
 *
 * @param base64 - The base64 string to decode.
 * @returns A promise that resolves to the decoded binary data.
 */
export declare const base64DecodeAsync: (base64: string) => Promise<ArrayBuffer>;

export {};
